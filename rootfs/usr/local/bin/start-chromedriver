#!/usr/bin/env bash
set -euo pipefail
for _ in $(seq 1 120); do
  if curl -fsS http://127.0.0.1:9222/json/version >/dev/null 2>&1; then
    exec /usr/bin/chromedriver --port=9515 --allowed-ips=
  fi
  sleep 0.5
done
echo >&2 "Chromium-Debug-Port wurde nicht rechtzeitig verfügbar."
exit 1
