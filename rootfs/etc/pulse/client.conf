# Verhindert, dass pactl/libpulse beim parallelen Containerstart selbstständig
# einen zweiten PulseAudio-Daemon startet. Der einzige Server wird von
# supervisord über start-pulseaudio verwaltet.
default-server = unix:/tmp/runtime-bridge/pulse/native
autospawn = no
enable-shm = no
